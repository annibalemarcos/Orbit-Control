[project]
name = "orbit-control"
version = "2.5.1"
description = "Aplicativo desktop para controlar Python, Node.js, BAT/CMD e outros processos."
requires-python = ">=3.10"
dependencies = [
  "PySide6>=6.8,<7",
  "psutil>=6.1,<8",
]

[tool.ruff]
line-length = 120
target-version = "py310"
